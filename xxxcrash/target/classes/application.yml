spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  datasource:
    url: "jdbc:mysql://localhost:3306/mydb"
    username: "root"
    password: "root"
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  error:
    include-message: always
  port: 8081